<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.AdminMapper">

    <select id="selectAll" resultType="com.mall.entity.Admin">
        SELECT * FROM admin ORDER BY id
    </select>

    <select id="selectById" resultType="com.mall.entity.Admin">
        SELECT * FROM admin WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultType="com.mall.entity.Admin">
        SELECT * FROM admin WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.mall.entity.Admin" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin (username, password, real_name, status, last_login_time, create_time, update_time)
        VALUES (#{username}, #{password}, #{realName}, #{status}, #{lastLoginTime}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.mall.entity.Admin">
        UPDATE admin
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE admin SET password = #{password}, update_time = NOW() WHERE id = #{id}
    </update>

</mapper>
