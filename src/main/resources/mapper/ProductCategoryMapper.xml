<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.ProductCategoryMapper">

    <select id="selectAll" resultType="com.mall.entity.ProductCategory">
        SELECT * FROM product_category ORDER BY sort_order
    </select>

    <select id="selectByParentId" resultType="com.mall.entity.ProductCategory">
        SELECT * FROM product_category WHERE parent_id = #{parentId} ORDER BY sort_order
    </select>

    <select id="selectById" resultType="com.mall.entity.ProductCategory">
        SELECT * FROM product_category WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.mall.entity.ProductCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_category (name, parent_id, sort_order, icon, status, create_time, update_time)
        VALUES (#{name}, #{parentId}, #{sortOrder}, #{icon}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.mall.entity.ProductCategory">
        UPDATE product_category
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

</mapper>
