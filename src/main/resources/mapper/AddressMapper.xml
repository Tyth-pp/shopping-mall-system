<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.AddressMapper">

    <select id="selectByUserId" resultType="com.mall.entity.Address">
        SELECT * FROM address WHERE user_id = #{userId} ORDER BY is_default DESC, create_time DESC
    </select>

    <select id="selectById" resultType="com.mall.entity.Address">
        SELECT * FROM address WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.mall.entity.Address" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO address (user_id, receiver_name, receiver_phone, province, city, district, detail, is_default, create_time, update_time)
        VALUES (#{userId}, #{receiverName}, #{receiverPhone}, #{province}, #{city}, #{district}, #{detail}, #{isDefault}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.mall.entity.Address">
        UPDATE address
        <set>
            <if test="receiverName != null">receiver_name = #{receiverName},</if>
            <if test="receiverPhone != null">receiver_phone = #{receiverPhone},</if>
            <if test="province != null">province = #{province},</if>
            <if test="city != null">city = #{city},</if>
            <if test="district != null">district = #{district},</if>
            <if test="detail != null">detail = #{detail},</if>
            <if test="isDefault != null">is_default = #{isDefault},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM address WHERE id = #{id}
    </delete>

    <update id="clearDefaultByUserId">
        UPDATE address SET is_default = 0 WHERE user_id = #{userId}
    </update>

    <update id="setDefault">
        UPDATE address SET is_default = 1 WHERE id = #{id}
    </update>

</mapper>
